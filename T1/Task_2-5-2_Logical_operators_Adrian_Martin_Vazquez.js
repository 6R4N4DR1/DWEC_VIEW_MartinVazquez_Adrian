// 1. Explain each otuput of the following code
Console.log( null || 2 || undefined ); // It gives 2, the log checks the first value is null so it goes to the next value because the operator OR checks the next value if the none is truth so it gives 2
Console.log( 1 && null && 2 ); // 
Console.log( null || 2 && 3 || 4 );
let x = 3; // Declares
console.log((x < 5) && (x > 0)); 
console.log((x < 5) && (x > 6));
console.log((x > 2) || (x > 5));
console.log((x > 3) || (x < 0));
console.log(!(x == 3));
console.log(!(x < 2));
